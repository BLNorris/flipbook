<!-- <% i = 0 %>
<% @users.each do |u| %>
  <% i += 1%>
  <div class="show_flipbk<%= i %>">
    <% #photo = u.photos.find(u.id) %>
    <img src="<%= u.photos %>" alt="<%=u.name%>">
  </div>
  <% i = 0 if i == 3 %>
<% end %> -->
<!DOCTYPE html>
<html>
<body>
  
<div id="tagline">
    <h1>flipbook</h1>
    <h2>Animate your process.</h2>
</div>

<div class="signup_form">
  
    <% if !current_user %>
      <%= render :partial => "form" %>
    <% else %>
      <%= link_to(current_user.name, user_url(current_user.id)) %>
    <% end %>
    
</div>

</body>
</html>